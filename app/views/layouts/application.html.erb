<!DOCTYPE html>
<html>
<head>
  <title><%= @page_title ? "#{@page_title}::University Catalog | The University of New Mexico" : "University Catalog | The University of New Mexico" %></title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= tinymce_assets %>
  <%= csrf_meta_tags %>
</head>
<body>

  <div class="outer-wrapper">

    <%= render 'layouts/unm_nav' %>
    <%= render 'layouts/header' %>

    <div class="inner-wrapper row">
      <div class="col-sm-2">
        <%= render 'layouts/nav' %>
      </div>
      <div class="col-sm-10">
        <%= render 'layouts/mini_header' %>
        <div class="row">
          <div class="main-body col-sm-9">
            <%= yield %>
          </div>
          <div class="col-sm-3">
          </div>
        </div>
      </div>
    </div>

    <%= render 'layouts/prefooter' %>
    <%= render 'layouts/footer' %>

  </div>

</body>
</html>
